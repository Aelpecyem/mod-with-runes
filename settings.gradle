pluginManagement {
    repositories {
        maven {
            name = 'Fabric'
            url = 'https://maven.fabricmc.net/'
        }
        mavenLocal() //blursed hierarchical json is built and published locally
        gradlePluginPortal()
    }
}
